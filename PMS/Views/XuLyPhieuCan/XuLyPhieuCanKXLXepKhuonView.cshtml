@using Newtonsoft.Json
@{
    ViewData["Title"] = "XuLyPhieuCanKXLXepKhuonView";
    Layout = "~/Views/Shared/_Layout.cshtml";
    //lấy danh sách roleid từ cookie
    var roleIds = PMS.Middlewares.AuthenticationHelpers.GetRoleIdsFromCookie(Context);
    // lấy danh sách fu func từ list roleId
    var rolePermistions = await PMS.Middlewares.AuthenticationHelpers.GetRolePermistionsAsync(Context, roleIds);
    var XuongId = @Context.Session.GetString("XuongId");
    string khuVuc = "XEPKHUONKHC";
    var listMaLos = ViewBag.listMaLos;
    var listMaLoaiCas = ViewBag.listMaLoaiCas;
    var listThanhPhams = ViewBag.listThanhPhams;
    var listSFLs = ViewBag.listSFLs;
    var listMaKhachHangs = ViewBag.listMaKhachHangs;
    var listCoiTams = ViewBag.listCoiTams;
    var DataNhanVien = ViewBag.DataNhanVien;
    var listXuongs = ViewBag.listXuongs;
}
<style>
    /* CSS */
    #modalThem .modal-dialog {
        max-width: 1000px; /* Thiết lập kích thước tùy chỉnh cho modal */
    }

    #modalSua .modal-dialog {
        max-width: 1000px; /* Thiết lập kích thước tùy chỉnh cho modal */
    }

    .displayValueNV {
        width: 100%; /* Thiết lập chiều rộng của combobox bằng 100% để nó lớn hơn và mở rộng ra toàn bộ kích thước của phần tử cha */
    }
    /* CSS cho headerTemplate */
    .head {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: #f0f0f0; /* Màu nền header */
        padding: 5px 10px; /* Khoảng cách bên trong header */
        font-weight: bold;
        border-bottom: 1px solid #ccc; /* Viền dưới header */
    }

        .head .ma, .head .ten, .head .ghichu {
            flex: 1;
            text-align: center; /* Căn giữa nội dung của các cột */
        }

    /* CSS cho itemTemplate */
    .item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 5px 10px; /* Khoảng cách bên trong mỗi item */
        border-bottom: 1px solid #eee; /* Viền dưới mỗi item */
    }

        .item .ma, .item .ten, .item .ghichu {
            flex: 1;
            text-align: center; /* Căn giữa nội dung của các cột */
        }

    span.e-input-group.e-control-wrapper.e-multi-column.e-ddl {
        height: 50px !important;
        border: 1px solid #d9dee3 !important;
        color: #697a8d !important;
        /*   font-weight: 400 !important;
                                                                                                                                    line-height: 1.53 !important; */
        background-clip: padding-box !important;
        min-height: calc(1.53em + 1.5rem + 2px) !important;
        padding: 0.75rem 1.25rem !important;
        font-size: 1rem !important;
        border-radius: 0.5rem !important;
    }

    div#inputpicker-wrapped-list.inputpicker-wrapped-list {
        width: 500px !important;
        max-height: 400px !important;
    }

    @@media (max-width: 768px) {
        div#inputpicker-wrapped-list.inputpicker-wrapped-list {
            width: 244px !important;
            max-height: 400px !important;
        }
    }

    .inputpicker-wrapped-list .inputpicker-active {
        background-color: #C0C0C0 !important;
    }
</style>
<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h2 class="mb-0">Phiếu Cân KXL Xếp Khuôn</h2>
                </div>
                <div class="card-body">
                    <div id="Grid"></div>

                    <div style="margin-top:20px;">
                        @{
                            var themPhieuCanXepKhuonKHC = rolePermistions.Where(x => x.Fu == "Xử Lý Phiếu Cân / Phiếu Cân KXL Xếp Khuôn" && x.Func == "Thêm Xử Lý Phiếu Cân / Phiếu Cân KXL Xếp Khuôn" && x.Status == 1).FirstOrDefault();
                            if (themPhieuCanXepKhuonKHC != null)
                            {
                                <button onclick="showInsert()" style="margin-right:5px" class="btn btn-outline-primary">Thêm</button>
                            }
                            var suaPhieuCanXepKhuonKHC = rolePermistions.Where(x => x.Fu == "Xử Lý Phiếu Cân / Phiếu Cân KXL Xếp Khuôn" && x.Func == "Sửa Xử Lý Phiếu Cân / Phiếu Cân KXL Xếp Khuôn" && x.Status == 1).FirstOrDefault();
                            if (suaPhieuCanXepKhuonKHC != null)
                            {
                                <button onclick="showUpdate()" style="margin-right:5px" class="btn btn-outline-warning"><i class='bx bx-edit-alt'></i>Sửa</button>
                            }
                            var xoaPhieuCanXepKhuonKHC = rolePermistions.Where(x => x.Fu == "Xử Lý Phiếu Cân / Phiếu Cân KXL Xếp Khuôn" && x.Func == "Xoá Xử Lý Phiếu Cân / Phiếu Cân KXL Xếp Khuôn" && x.Status == 1).FirstOrDefault();
                            if (xoaPhieuCanXepKhuonKHC != null)
                            {
                                <button onclick="showDelete()" style="margin-right:5px" class="btn btn-outline-danger"><i class='bx bx-trash'></i>Xoá</button>
                            }
                            var importPhieuCanXepKhuonKHC = rolePermistions.Where(x => x.Fu == "Xử Lý Phiếu Cân / Phiếu Cân KXL Xếp Khuôn" && x.Func == "Import Xử Lý Phiếu Cân / Phiếu Cân KXL Xếp Khuôn" && x.Status == 1).FirstOrDefault();
                            if (importPhieuCanXepKhuonKHC != null)
                            {
                                <button onclick="import()" style="margin-right:5px" class="btn btn-outline-info"><i class='bx bx-import'></i>Import</button>
                            }
                            var exportPhieuCanXepKhuonKHC = rolePermistions.Where(x => x.Fu == "Xử Lý Phiếu Cân / Phiếu Cân KXL Xếp Khuôn" && x.Func == "Export Xử Lý Phiếu Cân / Phiếu Cân KXL Xếp Khuôn" && x.Status == 1).FirstOrDefault();
                            if (exportPhieuCanXepKhuonKHC != null)
                            {
                                <button onclick="export()" style="margin-right:5px" class="btn btn-outline-secondary"><i class='bx bx-export'></i>Export Excel</button>
                            }
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="modal fade" id="modalThem" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Thêm Phiếu Cân</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="card">
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">STT</label>
                                    <input type="text" id="sttXLPCXKKHC" class="form-control form-control-lg" disabled />
                                </div>
                            </div>
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">Giờ</label>
                                    <input type="time" id="gioXLPCXKKHC" class="form-control form-control-lg" placeholder="Chọn giờ..." disabled />
                                </div>
                            </div>
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">Lô</label>
                                    <select id="maLoXLPCXKKHC" class="form-control form-control-lg">
                                        <option value="">Chọn lô</option>
                                        @foreach (var item in listMaLos)
                                        {
                                            <option value="@item.Item2">@item.Item2</option>
                                        }
                                    </select>
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">Loại Cá</label>
                                    <select id="maLoaiCaXLPCXKKHC" class="form-control form-control-lg">
                                        <option value="">Chọn loại cá</option>
                                        @foreach (var item in listMaLoaiCas)
                                        {
                                            <option value="@item.Ma">@item.Ten</option>
                                        }
                                    </select>
                                </div>
                            </div>
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">Thành Phẩm</label>
                                    <select id="maThanhPhamXLPCXKKHC" class="form-control form-control-lg">
                                        <option value="">Chọn thành phẩm</option>
                                        @foreach (var item in listThanhPhams)
                                        {
                                            <option value="@item.Ma">@item.Ten</option>
                                        }
                                    </select>
                                </div>
                            </div>
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">Size</label>
                                    <select id="maSizeXLPCXKKHC" class="form-control form-control-lg">
                                        <option value="">Chọn size</option>
                                        @foreach (var item in listSFLs)
                                        {
                                            <option value="@item.Ma">@item.Ten</option>
                                        }
                                    </select>
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">Khách Hàng</label>
                                    <select id="maKhachHangXLPCXKKHC" class="form-control form-control-lg">
                                        <option value="">Chọn khách hàng</option>
                                        @foreach (var item in listMaKhachHangs)
                                        {
                                            <option value="@item.Ma">@item.Ten</option>
                                        }
                                    </select>
                                </div>
                            </div>
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">Cối Tạm</label>
                                    <select id="maCoiTamXLPCXKKHC" class="form-control form-control-lg">
                                        <option value="">Chọn cối tạm</option>
                                        @foreach (var item in listCoiTams)
                                        {
                                            <option value="@item.Ma">@item.Ten</option>
                                        }
                                    </select>
                                </div>
                            </div>
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">Block</label>
                                    <input type="number" class="form-control form-control-lg" id="blockXLPCXKKHC" placeholder="Nhập số Block" />
                                </div>
                            </div>
                        </div>
                        <div class="row">

                            <div class="col-md-6 mb-4">
                                <div class="form-outline">
                                    <label class="form-label">Nhân Viên</label>
                                    @* <input class="form-control form-control-lg" id="maNhanVienXLPCXKKHC" placeholder="Chọn nhân viên" /> *@
                                    <ejs-combobox id="maNhanVienXLPCXKKHC"
                                                  open="onComboBoxOpen"
                                                  dataBound="isCheckScroll"
                                                  dataSource="@DataNhanVien"
                                                  cssClass="e-multi-column"
                                                  filtering="onfiltering"
                                                  allowFiltering="true"
                                                  placeholder="Chọn nhân viên"
                                                  popupHeight="400px"
                                                  enableVirtualization="true"
                                                  headerTemplate="@Html.Raw("<table><tr><th class='e-text-center' style='width: 75px;'>Mã</th><th>Tên</th><th>Nhóm</th></tr></table>")"
                                                  itemTemplate="@Html.Raw("<table><tbody><tr><td class='e-text-center' style='width: 75px;'>${MaNhanVien}</td><td>${Name}</td><td>${DeptName0}</td></tr> </tbody></table>")">
                                        <e-combobox-fields text="Name" value="MaNhanVien"></e-combobox-fields>
                                    </ejs-combobox>
                                </div>
                            </div>
                            <div class="col-md-6 mb-4">
                                <div class="form-outline">
                                    <label class="form-label">Trọng Lượng</label>
                                    <input type="number" min="0" step=".001" class="form-control form-control-lg" id="trongLuongXLPCXKKHC" placeholder="0.000" />
                                </div>
                            </div>
                        </div>
                        <div class="row mt-4 pt-2">
                            <div class="col-12">
                                <button onclick="insert()" class="btn btn-primary w-100 position-relative" type="submit" id="">
                                    <div class=" align-items-center">
                                        <span id="txtThem">Thêm</span>
                                        <div id="loadingIndicator" class=" spinner-border text-secondary position-absolute" role="status" style="left: 0;right: 0;top: 0;bottom: 0;margin: auto; display:none">
                                            <span class="visually-hidden">Đang tải...</span>
                                        </div>
                                    </div>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="modal fade" id="modalSua" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Sửa Phiếu Cân</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="card">
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">STT</label>
                                    <input type="text" id="sttXLPCXKKHCSua" class="form-control form-control-lg" disabled />
                                </div>
                            </div>
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">Giờ</label>
                                    <input type="time" id="gioXLPCXKKHCSua" class="form-control form-control-lg" placeholder="Chọn giờ..." disabled />
                                </div>
                            </div>
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">Lô</label>
                                    <select id="maLoXLPCXKKHCSua" class="form-control form-control-lg">
                                        <option value="">Chọn lô</option>
                                        @foreach (var item in listMaLos)
                                        {
                                            <option value="@item.Item2">@item.Item2</option>
                                        }
                                    </select>
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">Loại Cá</label>
                                    <select id="maLoaiCaXLPCXKKHCSua" class="form-control form-control-lg">
                                        <option value="">Chọn loại cá</option>
                                        @foreach (var item in listMaLoaiCas)
                                        {
                                            <option value="@item.Ma">@item.Ten</option>
                                        }
                                    </select>
                                </div>
                            </div>
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">Thành Phẩm</label>
                                    <select id="maThanhPhamXLPCXKKHCSua" class="form-control form-control-lg">
                                        <option value="">Chọn thành phẩm</option>
                                        @foreach (var item in listThanhPhams)
                                        {
                                            <option value="@item.Ma">@item.Ten</option>
                                        }
                                    </select>
                                </div>
                            </div>
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">Size</label>
                                    <select id="maSizeXLPCXKKHCSua" class="form-control form-control-lg">
                                        <option value="">Chọn size</option>
                                        @foreach (var item in listSFLs)
                                        {
                                            <option value="@item.Ma">@item.Ten</option>
                                        }
                                    </select>
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">Khách Hàng</label>
                                    <select id="maKhachHangXLPCXKKHCSua" class="form-control form-control-lg">
                                        <option value="">Chọn khách hàng</option>
                                        @foreach (var item in listMaKhachHangs)
                                        {
                                            <option value="@item.Ma">@item.Ten</option>
                                        }
                                    </select>
                                </div>
                            </div>
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">Cối Tạm</label>
                                    <select id="maCoiTamXLPCXKKHCSua" class="form-control form-control-lg">
                                        <option value="">Chọn cối tạm</option>
                                        @foreach (var item in listCoiTams)
                                        {
                                            <option value="@item.Ma">@item.Ten</option>
                                        }
                                    </select>
                                </div>
                            </div>
                            <div class="col-md-4 mb-2">
                                <div class="form-outline">
                                    <label class="form-label">Block</label>
                                    <input type="number" class="form-control form-control-lg" id="blockXLPCXKKHCSua" placeholder="Nhập số Block" />
                                </div>
                            </div>
                        </div>
                        <div class="row">

                            <div class="col-md-6 mb-4">
                                <div class="form-outline">
                                    <label class="form-label">Nhân Viên</label>
                                    @* <input class="form-control form-control-lg" id="maNhanVienXLPCXKKHCSua" placeholder="Chọn nhân viên" /> *@
                                    <ejs-combobox id="maNhanVienXLPCXKKHCSua"
                                                  open="onComboBoxOpen"
                                                  dataBound="isCheckScroll"
                                                  dataSource="@DataNhanVien"
                                                  cssClass="e-multi-column"
                                                  filtering="onfiltering"
                                                  allowFiltering="true"
                                                  placeholder="Chọn nhân viên"
                                                  popupHeight="400px"
                                                  enableVirtualization="true"
                                                  headerTemplate="@Html.Raw("<table><tr><th class='e-text-center' style='width: 75px;'>Mã</th><th>Tên</th><th>Nhóm</th></tr></table>")"
                                                  itemTemplate="@Html.Raw("<table><tbody><tr><td class='e-text-center' style='width: 75px;'>${MaNhanVien}</td><td>${Name}</td><td>${DeptName0}</td></tr> </tbody></table>")">
                                        <e-combobox-fields text="Name" value="MaNhanVien"></e-combobox-fields>
                                    </ejs-combobox>
                                </div>
                            </div>
                            <div class="col-md-6 mb-4">
                                <div class="form-outline">
                                    <label class="form-label">Trọng Lượng</label>
                                    <input type="number" min="0" step=".001" class="form-control form-control-lg" id="trongLuongXLPCXKKHCSua" placeholder="0.000" disabled/>
                                </div>
                            </div>
                        </div>
                        <div class="row mt-4 pt-2">
                            <div class="col-12">
                                <button onclick="update()" class="btn btn-primary w-100 position-relative" type="submit" id="">
                                    <div class=" align-items-center">
                                        <span id="txtSua">Sửa</span>
                                        <div id="loadingIndicatorSua" class=" spinner-border text-secondary position-absolute" role="status" style="left: 0;right: 0;top: 0;bottom: 0;margin: auto; display:none">
                                            <span class="visually-hidden">Đang tải...</span>
                                        </div>
                                    </div>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="modal fade" id="modalChuyenXuong" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Chọn Xưởng Muốn Chuyển</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="card">
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-12 mb-12 ">
                                <div class="form-outline">
                                    <label class="form-label">Xưởng</label>
                                    <select id="maXuongChuyenXuongXLPCXKKHC" class="form-control form-control-lg">
                                        <option disabled selected>Chọn Xưởng</option>
                                        @foreach (var item in listXuongs)
                                        {
                                            <option value="@item.Ma">@item.Ten</option>
                                        }
                                    </select>
                                </div>
                            </div>
                        </div>
                        <div class="row mt-4 pt-2">
                            <div class="col-12">
                                <button onclick="changeDepartment()" class="btn btn-primary w-100 position-relative" type="submit" id="">
                                    <div class=" align-items-center">
                                        <span id="txtChuyenXuongXLPCXKKHC">Xác Nhận</span>
                                        <div id="loadingIndicatorChuyenXuongXLPCXKKHC" class=" spinner-border text-secondary position-absolute" role="status" style="left: 0;right: 0;top: 0;bottom: 0;margin: auto; display:none">
                                            <span class="visually-hidden">Đang tải...</span>
                                        </div>
                                    </div>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="modal fade" id="modalChuyenSize" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Chọn Size Muốn Chuyển</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="card">
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-12 mb-12 ">
                                <div class="form-outline">
                                    <label class="form-label">Size</label>
                                    <select id="maSizeChuyenSizeXLPCXKKHC" class="form-control form-control-lg">
                                        <option disabled selected>Chọn Size</option>
                                        @foreach (var item in listSFLs)
                                        {
                                            <option value="@item.Ma">@item.Ten</option>
                                        }
                                    </select>
                                </div>
                            </div>
                        </div>
                        <div class="row mt-4 pt-2">
                            <div class="col-12">
                                <button onclick="changeSize()" class="btn btn-primary w-100 position-relative" type="submit" id="">
                                    <div class=" align-items-center">
                                        <span id="txtChuyenSizeXLPCXKKHC">Xác Nhận</span>
                                        <div id="loadingIndicatorChuyenSizeXLPCXKKHC" class=" spinner-border text-secondary position-absolute" role="status" style="left: 0;right: 0;top: 0;bottom: 0;margin: auto; display:none">
                                            <span class="visually-hidden">Đang tải...</span>
                                        </div>
                                    </div>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="modal fade" id="modalChuyenThanhPham" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Chọn Thành Phẩm Muốn Chuyển</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="card">
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-12 mb-12 ">
                                <div class="form-outline">
                                    <label class="form-label">Thành Phẩm</label>
                                    <select id="maThanhPhamChuyenThanhPhamXLPCXKKHC" class="form-control form-control-lg">
                                        <option disabled selected>Chọn Thành Phẩm</option>
                                        @foreach (var item in listThanhPhams)
                                        {
                                            <option value="@item.Ma">@item.Ten</option>
                                        }
                                    </select>
                                </div>
                            </div>
                        </div>
                        <div class="row mt-4 pt-2">
                            <div class="col-12">
                                <button onclick="changeThanhPham()" class="btn btn-primary w-100 position-relative" type="submit" id="">
                                    <div class=" align-items-center">
                                        <span id="txtChuyenThanhPhamXLPCXKKHC">Xác Nhận</span>
                                        <div id="loadingIndicatorChuyenThanhPhamXLPCXKKHC" class=" spinner-border text-secondary position-absolute" role="status" style="left: 0;right: 0;top: 0;bottom: 0;margin: auto; display:none">
                                            <span class="visually-hidden">Đang tải...</span>
                                        </div>
                                    </div>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<input type="hidden" id="maNhanVienXLPCXKKHCHHidden" />
<input type="hidden" id="sttXLPCXKKHCHHidden" />
<input type="hidden" id="sttBTPXLPCXKKHCHHidden" />
<input type="hidden" id="maMayCanXLPCXKKHCHHidden" />


<script src="~/assets/vendor/libs/inputpicker/jquery.inputpicker.js?v3"></script>
<script>

    function onfiltering(e) {
        var query = new ej.data.Query().select(['Name', 'MaNhanVien', 'DeptName0']); // Chỉ lấy 100 phần tử đầu tiên
        var predicateQuery = query.where(new ej.data.Predicate('MaNhanVien', 'contains', e.text, true).or('Name', 'contains', e.text, true));
        query = (e.text !== '') ? predicateQuery : query;
        e.updateData(@Html.Raw(JsonConvert.SerializeObject(@ViewBag.DataNhanVien)), query);
        isCheckScroll.call(this);
    }

    function onComboBoxOpen(args) {
        var comboBox = args.popup; // Lấy popup của combobox
        if (comboBox) {
            // Kiểm tra nếu dataSource tồn tại
            if (comboBox.dataSource) {
                // Chỉ hiển thị 10 phần tử đầu tiên
                comboBox.dataSource = comboBox.dataSource.slice(0, 10);
            }
        }
    }

    function isCheckScroll() {
        if (this.popupObj && this.popupObj.element && this.liCollections.length > 0) {
            var offsetHeight = this.popupObj.element.querySelector('.e-content').offsetHeight;
            var contentHeight = offsetHeight < this.liCollections[0].offsetHeight * this.liCollections.length;
            if (contentHeight || offsetHeight === 0) {
                this.popupObj.element.classList.add('e-scroller');
            } else {
                this.popupObj.element.classList.remove('e-scroller');
            }
        }
    }

</script>
<script>
    // $(document).ready(function () {
    //     loadGridData();
    // });
    let selectedIdsString = ""; // Khai báo một chuỗi để lưu trữ MaNhanVien đã chọn
    let numRowSelect = 0;
    var grid;
    function creatTable(data) {
        var dataManager = new ej.data.DataManager({
            json: data,
            adaptor: new ej.data.JsonAdaptor()
        });
        if (grid!= null) {
            grid.dataSource = dataManager;
            grid.refresh();
        } else {

            var contextMenuItems = [
                { text: 'Xem ghi chú', id: 'viewNote' },
                { text: 'Chuyển xưởng', id: 'changeDepartment' },
                { text: 'Chuyển size', id: 'changeSize' },
                { text: 'Chuyển thành phẩm', id: 'changeThanhPham' },
            ];
            grid = new ej.grids.Grid({
                dataSource: dataManager,
                allowPaging: true,
                pageSettings: { pageSize: 500 },
                allowSelection: true,
                allowFiltering: true,
                allowSorting: true,
                allowResizing: true,
                //enableVirtualization: true,
                //loadingIndicator: { indicatorType: 'Shimmer' },
                filterSettings: { type: 'Excel' },
                //selectionSettings: { persistSelection: true, type: "Multiple", checkboxOnly: true },
                selectionSettings: { persistSelection: true },
                rowSelected: selectionChanged,
                rowDeselected: unSelectionChanged,
                height: 400,
                contextMenuItems: contextMenuItems,
                contextMenuClick: contextMenuClickHandler,
                columns: [
                    { type: "checkbox", field: "", allowFiltering: false, allowSorting: false, width: '50' },
                    { field: 'STT', headerText: 'STT', width: 150 },
                    { field: 'Gio', headerText: 'Giờ', width: 150 },
                    { field: 'MaNhanVien', headerText: 'Mã Nhân Viên', width: 150 },
                    { field: 'MaHoSo', headerText: 'Mã Số', width: 150 },
                    { field: 'NhanVienName', headerText: 'Tên Nhân Viên', width: 150 },
                    { field: 'MaLo', headerText: 'Lô', width: 150 },
                    { field: 'LoaiCaName', headerText: 'Loại Cá', width: 150 },
                    { field: 'ThanhPhamName', headerText: 'Thành Phẩm', width: 150 },
                    { field: 'SizeName', headerText: 'Size', width: 150 },
                    { field: 'KhachHangName', headerText: 'Khách Hàng', width: 150 },
                    { field: 'CoiTamName', headerText: 'Cối Tạm', width: 150 },
                    { field: 'TrongLuong', headerText: 'Trọng Lượng', type: 'number', width: 150 },
                    { field: 'DaXacNhan', headerText: 'Đã Xác Nhận', width: 100, displayAsCheckBox: 'true', textAlign: 'Center' },
                    { field: 'Block', headerText: 'Block', type: 'number', width: 150 },
                    { field: 'XuongName', headerText: 'Xưởng', width: 150 },
                    { field: 'MaMayCan', headerText: 'Máy Cân', width: 150 },
                ],
                aggregates: [{
                    columns: [{
                        type: 'Sum',
                        field: 'TrongLuong',
                        format: 'N2',
                        footerTemplate: 'Tổng TL: ${Sum} Kg'
                    }]
                }]
            });
            grid.appendTo('#Grid');
        }
    }

    creatTable();
    function contextMenuClickHandler(args) {
        var rowData = args.rowInfo.rowData;
        if (rowData) {
            switch (args.item.id) {
                case 'viewNote':
                    var ghiChu = rowData.GhiChu;
                    if (ghiChu) {
                        // Thay thế dấu phẩy bằng thẻ <br> để tạo xuống hàng
                        ghiChu = ghiChu.replace(/,/g, '<br>');
                        bootbox.alert({
                            title: "Ghi chú",
                            message: ghiChu,
                            callback: function () { }
                        });
                    } else {
                        bootbox.alert("Không có ghi chú nào cho hàng này.");
                    }
                    break;
                case 'changeDepartment':
                    $.ajax({
                        url: "/XuLyPhieuCan/CheckQuyenXepKhuonKHC",
                        data: { typeOption: "CHUYENXUONG" },
                        type: 'GET',
                        dataType: 'json',
                        success: function (rs) {
                            if (rs.isSuccess === true) {
                                bootbox.confirm({
                                    title: "Thông báo!",
                                    message: "Bạn có chắc muốn chuyển xưởng của " + numRowSelect + " phiếu cân đang chọn!",
                                    buttons: {
                                        confirm: {
                                            label: 'OK',
                                            className: 'btn btn-primary'
                                        }
                                    },
                                    callback: function (result) {
                                        if (result) {
                                            $('#modalChuyenXuong').modal('show');
                                        }
                                    }
                                });
                            }
                            else {
                                toastOptionNotyfi();
                                toastr["warning"](rs.Messages, "Thông báo");
                            }
                        },
                        error: function (error) {
                            toastOptionNotyfi();
                            toastr["error"]('Đã xảy ra lỗi: ' + error, "Thông báo");
                        }
                    });

                    break;
                case 'changeSize':
                    $.ajax({
                        url: "/XuLyPhieuCan/CheckQuyenXepKhuonKHC",
                        data: { typeOption: "CHUYENSIZE" },
                        type: 'GET',
                        dataType: 'json',
                        success: function (rs) {
                            if (rs.isSuccess === true) {
                                bootbox.confirm({
                                    title: "Thông báo!",
                                    message: "Bạn có chắc muốn chuyển sỉze của " + numRowSelect + " phiếu cân đang chọn!",
                                    buttons: {
                                        confirm: {
                                            label: 'OK',
                                            className: 'btn btn-primary'
                                        }
                                    },
                                    callback: function (result) {
                                        if (result) {
                                            $('#modalChuyenSize').modal('show');
                                        }
                                    }
                                });
                            }
                            else {
                                toastOptionNotyfi();
                                toastr["warning"](rs.Messages, "Thông báo");
                            }
                        },
                        error: function (error) {
                            toastOptionNotyfi();
                            toastr["error"]('Đã xảy ra lỗi: ' + error, "Thông báo");
                        }
                    });

                    break;
                case 'changeThanhPham':
                    $.ajax({
                        url: "/XuLyPhieuCan/CheckQuyenXepKhuonKHC",
                        data: { typeOption: "CHUYENTHANHPHAM" },
                        type: 'GET',
                        dataType: 'json',
                        success: function (rs) {
                            if (rs.isSuccess === true) {
                                bootbox.confirm({
                                    title: "Thông báo!",
                                    message: "Bạn có chắc muốn chuyển thành phẩm của " + numRowSelect + " phiếu cân đang chọn!",
                                    buttons: {
                                        confirm: {
                                            label: 'OK',
                                            className: 'btn btn-primary'
                                        }
                                    },
                                    callback: function (result) {
                                        if (result) {
                                            $('#modalChuyenThanhPham').modal('show');
                                        }
                                    }
                                });
                            }
                            else {
                                toastOptionNotyfi();
                                toastr["warning"](rs.Messages, "Thông báo");
                            }
                        },
                        error: function (error) {
                            toastOptionNotyfi();
                            toastr["error"]('Đã xảy ra lỗi: ' + error, "Thông báo");
                        }
                    });

                    break;
                default:
                    break;
            }
        }
    }


    function unSelectionChanged(args) {
        let deselectedRecords = args.data ? args.data : []; // Lấy các bản ghi được bỏ chọn

        // Kiểm tra xem deselectedRecords có phải là một mảng không
        if (Array.isArray(deselectedRecords)) {
            // Nếu là một mảng rỗng hoặc không tồn tại
            if (deselectedRecords.length === 0) {
                selectedIdsString = "";
                numRowSelect = 0; // Reset số dòng đã chọn
                //alert(selectedIdsString);
                return;
            }
            // Lặp qua từng bản ghi được bỏ chọn
            for (let i = 0; i < deselectedRecords.length; i++) {
                let deselectedSTT = deselectedRecords[i].STT;
                let deselectedMaMayCan = deselectedRecords[i].MaMayCan;
                let deselectedMaXuong = deselectedRecords[i].MaXuong;
                let deselectedId = deselectedSTT + "," + deselectedMaMayCan + "," + deselectedMaXuong;

                // Kiểm tra xem MaNhanVien đã có trong chuỗi chưa trước khi loại bỏ
                if (selectedIdsString.includes(deselectedId)) {
                    selectedIdsString = selectedIdsString.replace(deselectedId + "|", ""); // Loại bỏ MaNhanVien khỏi chuỗi
                    numRowSelect--; // Giảm số dòng đã chọn
                }
            }
        } else {
            // Nếu deselectedRecords không phải là một mảng
            let deselectedSTT = deselectedRecords.STT;
            let deselectedMaMayCan = deselectedRecords.MaMayCan;
            let deselectedMaXuong = deselectedRecords.MaXuong;
            let deselectedId = deselectedSTT + "," + deselectedMaMayCan + "," + deselectedMaXuong;

            // Kiểm tra xem MaNhanVien đã có trong chuỗi chưa trước khi loại bỏ
            if (selectedIdsString.includes(deselectedId)) {
                selectedIdsString = selectedIdsString.replace(deselectedId + "|", ""); // Loại bỏ MaNhanVien khỏi chuỗi
                numRowSelect--; // Giảm số dòng đã chọn
            }
            //alert(selectedIdsString);
        }
        // Hiển thị số dòng đã chọn nếu cần
        //alert(numRowSelect);
    }

    function selectionChanged(args) {
        let selectedRecords = args.data ? args.data : []; // Lấy các bản ghi được chọn
        // Giả định có một thuộc tính isHeader để xác định xem dữ liệu có phải từ header hay không
        if (selectedRecords.length == 0) {
            // Lấy toàn bộ Mã Nhân viên từ grid vào selectedIdsString
            let filteredRecords = grid.getCurrentViewRecords(); // Lấy các bản ghi đã được lọc
            selectedIdsString = ""; // Reset selectedIdsString trước khi thêm dữ liệu mới
            numRowSelect = filteredRecords.length; // Cập nhật số dòng đã chọn
            filteredRecords.forEach(record => {
                selectedIdsString += record.STT + ","; // Thêm STT vào chuỗi, phân tách bằng dấu phẩy
                selectedIdsString += record.MaMayCan + ""; // Thêm MayTP vào chuỗi, phân tách bằng dấu phẩy
                selectedIdsString += record.MaXuong + "|";
            });

            // Loại bỏ dấu | cuối cùng
            selectedIdsString = selectedIdsString.slice(0, -1);
            //alert(selectedIdsString);
            //alert(numRowSelect);
            return;
        }
        let selectedSTT = selectedRecords.STT;
        let selectedMaMayCan = selectedRecords.MaMayCan;
        let selectedMaXuong = selectedRecords.MaXuong;
        // Kiểm tra xem MaNhanVien đã được thêm vào chuỗi chưa trước khi thêm vào
        if (!selectedIdsString.includes(selectedSTT)) {
            selectedIdsString += selectedSTT + ","; // Thêm STT vào chuỗi, phân tách bằng dấu phẩy
            selectedIdsString += selectedMaMayCan + ","; // Thêm MayTP vào chuỗi, phân tách bằng dấu phẩy
            selectedIdsString += selectedMaXuong + "|"; // Thêm MaXuong vào chuỗi, phân tách bằng dấu chấm than, ngăn cách các bản ghi bằng dấu |

            numRowSelect++; // Tăng số dòng đã chọn
        }
        // Loại bỏ dấu | cuối cùng
        //selectedIdsString = selectedIdsString.slice(0, -1);
        //alert(selectedIdsString);
        // Hiển thị số dòng đã chọn nếu cần
        //alert(numRowSelect);
    }
    function ClearData(GridId) {
        try {
            var gridObj = document.getElementById(GridId).ej2_instances[0];
            if(gridObj)
                gridObj.dataSource = [];
        } catch (e) {
            Console.log(e);
        }

    }
    function reloadDataXLPCXKKHC() {
        ClearData("Grid");
        var btn = document.getElementById('RefreshButtonXLPC').ej2_instances[0];
        var content = btn.content;
        btn.content = "Đang Tải ...";
        var dateTime = $('#dateTimeXLPC').val();
        var xuongId = @XuongId;
        var typeValue = "@khuVuc";
        $.ajax({
            url: "@Url.Action("Reload", "XuLyPhieuCan")",
            type: "GET",
            data: { dateTime: dateTime, xuongId: xuongId, typeValue: typeValue },
            success: function (response) {
                if (response.Success) {
                    creatTable(response.Data);
                } else {
                    creatTable();
                    console.error(response.Messages);
                    toastOptionNotyfi();
                    toastr["error"](response.Messages, "Không có dữ liệu!");
                }
            },
            error: function (error) {
                toastOptionNotyfi();
                toastr["error"](error, "Thông báo");
            }
        }).always(function () {
            btn.content = content;
        });
    }
    document.getElementById('RefreshButtonXLPC').addEventListener("click", function () {
        reloadDataXLPCXKKHC();
    });
</script>
<script>
    function getCurrentTime() {
        let now = new Date();

        // Lấy các thành phần giờ, phút, giây và mili giây
        let hours = String(now.getHours()).padStart(2, '0');
        let minutes = String(now.getMinutes()).padStart(2, '0');
        let seconds = String(now.getSeconds()).padStart(2, '0');
        let milliseconds = String(now.getMilliseconds()).padStart(7, '0');

        // Định dạng thời gian theo yêu cầu
        let currentTime = `${hours}:${minutes}:${seconds}.${milliseconds}`;

        return currentTime;
    }
    function setFormattedTime(timeString) {
        // Chuyển đổi chuỗi thời gian sang đối tượng Date
        var time = new Date("1970-01-01T" + timeString);

        // Lấy giờ và phút
        var hours = time.getHours().toString().padStart(2, '0');
        var minutes = time.getMinutes().toString().padStart(2, '0');
        var seconds = time.getSeconds().toString().padStart(2, '0');

        // Định dạng lại chuỗi thời gian
        var formattedTime = hours + ":" + minutes;

        return formattedTime;
    }
    function showInsert() {
        $('#gioXLPCXKKHC').val(setFormattedTime(getCurrentTime()));
        // $.ajax({
        //     type: "GET",
        //     url: "/XuLyPhieuCan/GetNhanVien_XLPC",
        //     success: function (rs) {
        //         if (rs != null) {
        //             $('#maNhanVienXLPCXKKHC').inputpicker({
        //                 data: rs,
        //                 fields: [
        //                     { name: 'MaNhanVien', text: 'Mã Nhân Viên' },
        //                     { name: 'MaHoSo', text: 'Mã Hồ Sơ' },
        //                     { name: 'Name', text: 'Tên' },
        //                     { name: 'DeptName0', text: 'Nhóm' },
        //                 ],
        //                 headShow: true,
        //                 fieldText: 'Name',
        //                 fieldValue: 'MaNhanVien',
        //                 filterOpen: true
        //             });
        //         }
        //         else {
        //             toastOptionNotyfi();
        //             toastr["warning"](rs.Mesages, "Thông báo");
        //         }
        //     },
        //     error: function () {
        //         toastOptionNotyfi();
        //         toastr["error"]('Đã xảy ra lỗi: ' + error, "Thông báo");
        //     }
        // });
        var dateTime = $('#dateTimeXLPC').val();
        $.ajax({
            type: "GET",
            url: "/XuLyPhieuCan/CreatDefautNewXepKhuonKHC",
            data: {
                dateTime: dateTime,
                xuongId: @XuongId
                                                                                    },
            success: function (rs) {
                if (rs.isSuccess === true) {

                    $('#sttXLPCXKKHC').val(rs.STT);
                    $('#modalThem').modal('show');
                }
                else {
                    toastOptionNotyfi();
                    toastr["warning"](rs.Mesages, "Thông báo");
                }
            },
            error: function () {
                toastOptionNotyfi();
                toastr["error"]('Đã xảy ra lỗi: ' + error, "Thông báo");
            }
        });
    }
    function insert() {
        var dateTime = $('#dateTimeXLPC').val();
        var xuongId = @XuongId;
        var stt = $('#sttXLPCXKKHC').val();
        var gio = $('#gioXLPCXKKHC').val();
        var maLo = $('#maLoXLPCXKKHC').val();
        var maLoaiCa = $('#maLoaiCaXLPCXKKHC').val();
        var maThanhPham = $('#maThanhPhamXLPCXKKHC').val();
        var maSize = $('#maSizeXLPCXKKHC').val();
        var maKhachHang = $('#maKhachHangXLPCXKKHC').val();
        var maCoiTam = $('#maCoiTamXLPCXKKHC').val();
        var block = $('#blockXLPCXKKHC').val();
        //var maNhanVien = $('#maNhanVienXLPCXKKHC').val();
        var comboboxNhanVien = document.getElementById('maNhanVienXLPCXKKHC').ej2_instances[0];
        var maNhanVien = comboboxNhanVien.value;
        var trongLuong = $('#trongLuongXLPCXKKHC').val();
        document.getElementById("txtThem").style.visibility = "hidden";
        document.getElementById("loadingIndicator").style.display = "block";
        $.ajax({
            type: 'POST',
            url: '/XuLyPhieuCan/DoInsert_XepKhuonKHC',
            data: {
                dateTime: dateTime,
                xuongId: xuongId,
                stt: stt,
                gio: gio,
                maLo: maLo,
                maLoaiCa: maLoaiCa,
                maThanhPham: maThanhPham,
                maSize: maSize,
                maKhachHang: maKhachHang,
                maCoiTam: maCoiTam,
                block: block,
                maNhanVien: maNhanVien,
                trongLuong: trongLuong,
            },
            success: function (rs) {
                if (rs.isSuccess === true) {
                    toastOptionNotyfi();
                    toastr["success"](rs.Messages, "Thông báo");
                    $('#modalThem').modal('hide');
                    reloadDataXLPCXKKHC();
                } else {
                    toastOptionNotyfi();
                    toastr["warning"](rs.Messages, "Thông báo");
                }
            },
            error: function (error) {
                toastOptionNotyfi();
                toastr["error"]('Đã xảy ra lỗi: ' + error, "Thông báo");
            },
            complete: function () {
                document.getElementById("loadingIndicator").style.display = "none";
                document.getElementById("txtThem").style.visibility = "visible";
            }
        });
    }
</script>
<script>

    function showUpdate() {
        // $.ajax({
        //     type: "GET",
        //     url: "/XuLyPhieuCan/GetNhanVien_XLPC",
        //     success: function (rs) {
        //         if (rs != null) {
        //             $('#maNhanVienXLPCXKKHCSua').inputpicker({
        //                 data: rs,
        //                 fields: [
        //                     { name: 'MaNhanVien', text: 'Mã Nhân Viên' },
        //                     { name: 'MaHoSo', text: 'Mã Hồ Sơ' },
        //                     { name: 'Name', text: 'Tên' },
        //                     { name: 'DeptName0', text: 'Nhóm' },
        //                 ],
        //                 headShow: true,
        //                 fieldText: 'Name',
        //                 fieldValue: 'MaNhanVien',
        //                 filterOpen: true
        //             });
        //         }
        //         else {
        //             toastOptionNotyfi();
        //             toastr["warning"](rs.Mesages, "Thông báo");
        //         }
        //     },
        //     error: function () {
        //         toastOptionNotyfi();
        //         toastr["error"]('Đã xảy ra lỗi: ' + error, "Thông báo");
        //     }
        // });
        var dateTime = $('#dateTimeXLPC').val();
        $.ajax({
            url: "/XuLyPhieuCan/GetsByMa_XepKhuonKHC",
            data: { listInfoPhieuCan: selectedIdsString, ngay: dateTime },
            type: 'GET',
            dataType: 'json',
            success: function (rs) {
                if (rs.isSuccess === true) {
                    $('#sttXLPCXKKHCSua').val(rs.STT);
                    $('#gioXLPCXKKHCSua').val(rs.Gio);
                    $('#maLoXLPCXKKHCSua').val(rs.MaLo);
                    $('#maLoaiCaXLPCXKKHCSua').val(rs.MaLoaiCa);
                    $('#maThanhPhamXLPCXKKHCSua').val(rs.MaThanhPham);
                    $('#maSizeXLPCXKKHCSua').val(rs.MaSize);
                    $('#maKhachHangXLPCXKKHCSua').val(rs.MaKhachHang);
                    $('#maCoiTamXLPCXKKHCSua').val(rs.MaCoiTam);
                    $('#blockXLPCXKKHCSua').val(rs.Block);
                    //$('#maNhanVienXLPCXKKHCSua').val(rs.MaNhanVien);
                    var comboBoxObj = document.getElementById('maNhanVienXLPCXKKHCSua').ej2_instances[0];
                    // Sử dụng phương thức value để gán MaNhanVien
                    comboBoxObj.value = rs.MaNhanVien;

                    // Nếu cần, có thể dùng phương thức `dataBound` để gán giá trị sau khi dữ liệu đã được liên kết
                    comboBoxObj.dataBound = function () {
                        comboBoxObj.value = rs.MaNhanVien;
                    };
                    $('#trongLuongXLPCXKKHCSua').val(rs.TrongLuong);
                    $('#maMayCanXLPCXKKHCHHidden').val(rs.MaMayCan);
                    $('#modalSua').modal('show');
                }
                else {
                    toastOptionNotyfi();
                    toastr["warning"](rs.Mesages, "Thông báo");
                }
            },
            error: function (error) {
                toastOptionNotyfi();
                toastr["error"]('Đã xảy ra lỗi: ' + error, "Thông báo");
            }
        });

    }
    function update() {
        var ngay = $('#dateTimeXLPC').val();
        var xuongId = @XuongId;
        var stt = $('#sttXLPCXKKHCSua').val();
        var gio = $('#gioXLPCXKKHCSua').val();
        var maLo = $('#maLoXLPCXKKHCSua').val();
        var maLoaiCa = $('#maLoaiCaXLPCXKKHCSua').val();
        var maThanhPham = $('#maThanhPhamXLPCXKKHCSua').val();
        var maSize = $('#maSizeXLPCXKKHCSua').val();
        var maKhachHang = $('#maKhachHangXLPCXKKHCSua').val();
        var maCoiTam = $('#maCoiTamXLPCXKKHCSua').val();
        var block = $('#blockXLPCXKKHCSua').val();
        //var maNhanVien = $('#maNhanVienXLPCXKKHCSua').val();
        var comboboxNhanVien = document.getElementById('maNhanVienXLPCXKKHCSua').ej2_instances[0];
        var maNhanVien = comboboxNhanVien.value;
        var trongLuong = $('#trongLuongXLPCXKKHCSua').val();
        var maMayCan = $('#maMayCanXLPCXKKHCHHidden').val();
        document.getElementById("txtSua").style.visibility = "hidden";
        document.getElementById("loadingIndicatorSua").style.display = "block";
        $.ajax({
            url: '/XuLyPhieuCan/DoUpDate_XepKhuonKHC',
            data: {
                ngay: ngay,
                maXuong: xuongId,
                stt: stt,
                gio: gio,
                maLo: maLo,
                maLoaiCa: maLoaiCa,
                maThanhPham: maThanhPham,
                maSize: maSize,
                maKhachHang: maKhachHang,
                maCoiTam: maCoiTam,
                block: block,
                maNhanVien: maNhanVien,
                trongLuong: trongLuong,
                maMayCan: maMayCan
            },
            type: 'POST',
            dataType: 'json',
            success: function (rs) {
                if (rs.isSuccess === true) {
                    $('#modalSua').modal('hide');
                    reloadDataXLPCXKKHC();
                    toastOptionNotyfi();
                    toastr["success"](rs.Messages, "Thông báo");
                } else {
                    toastOptionNotyfi();
                    toastr["warning"](rs.Messages, "Thông báo");
                }
            },
            error: function (error) {
                toastOptionNotyfi();
                toastr["error"]('Đã xảy ra lỗi: ' + error.toString(), "Thông báo");
            },
            complete: function () {
                document.getElementById("loadingIndicatorSua").style.display = "none";
                document.getElementById("txtSua").style.visibility = "visible";
            }
        });
    }
</script>
<script>
    function showDelete() {
        var dateTime = $('#dateTimeXLPC').val();

        bootbox.confirm({
            title: "Thông báo!",
            message: "Bạn có chắc muốn xoá " + numRowSelect + " phiếu cân đang chọn!",
            buttons: {
                confirm: {
                    label: 'OK',
                    className: 'btn btn-primary'
                }
            },
            callback: function (result) {
                if (result) {
                    //Người dùng đã bấm OK, Gọi API xóa ở đây
                    $.ajax({
                        url: '/XuLyPhieuCan/DoDelete_XepKhuonKHC',
                        data: { listInfoPhieuCan: selectedIdsString, ngay: dateTime },
                        type: 'POST',
                        dataType: 'json',
                        success: function (rs) {
                            if (rs.isSuccess === true) {
                                reloadDataXLPCXKKHC();
                                toastOptionNotyfi();
                                toastr["success"](rs.Messages, "Thông báo");
                            } else {
                                toastOptionNotyfi();
                                toastr["warning"](rs.Messages, "Thông báo");
                            }
                        },
                        error: function (error) {
                            toastOptionNotyfi();
                            toastr["error"]('Đã xảy ra lỗi: ' + error.toString(), "Thông báo");
                        }
                    });
                }
            }
        });
    }
</script>
<script>
    function changeDepartment() {
        var maXuongChange = $('#maXuongChuyenXuongXLPCXKKHC').val();
        if (maXuongChange == "" || maXuongChange == null || maXuongChange == undefined) {
            toastOptionNotyfi();
            toastr["warning"]("Bạn chưa chọn xưởng!", "Thông báo");
            return;
        }
        var dateTime = $('#dateTimeXLPC').val();
        document.getElementById("txtChuyenXuongXLPCXKKHC").style.visibility = "hidden";
        document.getElementById("loadingIndicatorChuyenXuongXLPCXKKHC").style.display = "block";
        $.ajax({
            url: '/XuLyPhieuCan/ChuyenXuong_XepKhuonKHC',
            data: { listInfoPhieuCan: selectedIdsString, ngay: dateTime, maXuongChange: maXuongChange },
            type: 'POST',
            dataType: 'json',
            success: function (rs) {
                if (rs.isSuccess === true) {
                    $('#modalChuyenXuong').modal('hide');
                    reloadDataXLPCXKKHC();
                    toastOptionNotyfi();
                    toastr["success"](rs.Messages, "Thông báo");
                } else {
                    toastOptionNotyfi();
                    toastr["warning"](rs.Messages, "Thông báo");
                }
            },
            error: function (error) {
                toastOptionNotyfi();
                toastr["error"]('Đã xảy ra lỗi: ' + error.toString(), "Thông báo");
            },
            complete: function () {
                document.getElementById("loadingIndicatorChuyenXuongXLPCXKKHC").style.display = "none";
                document.getElementById("txtChuyenXuongXLPCXKKHC").style.visibility = "visible";
            }
        });
    }
    function changeSize() {
        var maSizeChange = $('#maSizeChuyenSizeXLPCXKKHC').val();
        if (maSizeChange == "" || maSizeChange == null || maSizeChange == undefined) {
            toastOptionNotyfi();
            toastr["warning"]("Bạn chưa chọn size!", "Thông báo");
            return;
        }
        var dateTime = $('#dateTimeXLPC').val();
        document.getElementById("txtChuyenSizeXLPCXKKHC").style.visibility = "hidden";
        document.getElementById("loadingIndicatorChuyenSizeXLPCXKKHC").style.display = "block";
        $.ajax({
            url: '/XuLyPhieuCan/ChuyenSize_XepKhuonKHC',
            data: { listInfoPhieuCan: selectedIdsString, ngay: dateTime, maSizeChange: maSizeChange },
            type: 'POST',
            dataType: 'json',
            success: function (rs) {
                if (rs.isSuccess === true) {
                    $('#modalChuyenSize').modal('hide');
                    reloadDataXLPCXKKHC();
                    toastOptionNotyfi();
                    toastr["success"](rs.Messages, "Thông báo");
                } else {
                    toastOptionNotyfi();
                    toastr["warning"](rs.Messages, "Thông báo");
                }
            },
            error: function (error) {
                toastOptionNotyfi();
                toastr["error"]('Đã xảy ra lỗi: ' + error.toString(), "Thông báo");
            },
            complete: function () {
                document.getElementById("loadingIndicatorChuyenSizeXLPCXKKHC").style.display = "none";
                document.getElementById("txtChuyenSizeXLPCXKKHC").style.visibility = "visible";
            }
        });
    }
    function changeThanhPham() {
        var maThanhPhamChange = $('#maThanhPhamChuyenThanhPhamXLPCXKKHC').val();
        if (maThanhPhamChange == "" || maThanhPhamChange == null || maThanhPhamChange == undefined) {
            toastOptionNotyfi();
            toastr["warning"]("Bạn chưa chọn thành phẩm!", "Thông báo");
            return;
        }
        var dateTime = $('#dateTimeXLPC').val();
        document.getElementById("txtChuyenThanhPhamXLPCXKKHC").style.visibility = "hidden";
        document.getElementById("loadingIndicatorChuyenThanhPhamXLPCXKKHC").style.display = "block";
        $.ajax({
            url: '/XuLyPhieuCan/ChuyenThanhPham_XepKhuonKHC',
            data: { listInfoPhieuCan: selectedIdsString, ngay: dateTime, maThanhPhamChange: maThanhPhamChange },
            type: 'POST',
            dataType: 'json',
            success: function (rs) {
                if (rs.isSuccess === true) {
                    $('#modalChuyenThanhPham').modal('hide');
                    reloadDataXLPCXKKHC();
                    toastOptionNotyfi();
                    toastr["success"](rs.Messages, "Thông báo");
                } else {
                    toastOptionNotyfi();
                    toastr["warning"](rs.Messages, "Thông báo");
                }
            },
            error: function (error) {
                toastOptionNotyfi();
                toastr["error"]('Đã xảy ra lỗi: ' + error.toString(), "Thông báo");
            },
            complete: function () {
                document.getElementById("loadingIndicatorChuyenThanhPhamXLPCXKKHC").style.display = "none";
                document.getElementById("txtChuyenThanhPhamXLPCXKKHC").style.visibility = "visible";
            }
        });
    }
</script>


