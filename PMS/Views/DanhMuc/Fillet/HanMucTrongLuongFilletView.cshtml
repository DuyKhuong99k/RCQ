
@{
    ViewData["Title"] = "HanMucTrongLuongFilletView";
    Layout = "~/Views/Shared/_Layout.cshtml";
    //lấy danh sách roleid từ cookie
    var roleIds = PMS.Middlewares.AuthenticationHelpers.GetRoleIdsFromCookie(Context);
    // lấy danh sách fu func từ list roleId
    var rolePermistions = await PMS.Middlewares.AuthenticationHelpers.GetRolePermistionsAsync(Context, roleIds);
    var listMaLos = ViewBag.listMaLos;
    var listTPFLs = ViewBag.listTPFLs;
    var listXuongs = ViewBag.listXuongs;
    var XuongIdHMTLFL = @Context.Session.GetString("XuongId");
}


    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h2 class="mb-0">Danh Sách Hạn Mức Trọng Lượng Fillet</h2>
                    @{
                        
                    }

                </div>
                <div class="card-body">
                    <div id="Grid"></div>
                    <div style="margin-top:20px;">
                        @{
                            var themHanMucTrongLuongFillet = rolePermistions.Where(x => x.Fu == "Danh Mục / Fillet / Hạn Mức Trọng Lượng" && x.Func == "Thêm Fillet / Hạn Mức Trọng Lượng" && x.Status == 1).FirstOrDefault();
                            if (themHanMucTrongLuongFillet != null)
                            {
                                <button onclick="showInsert()" type="button" class="btn btn-primary">Thêm</button>
                            }
                            var suaHanMucTrongLuongFillet = rolePermistions.Where(x => x.Fu == "Danh Mục / Fillet / Hạn Mức Trọng Lượng" && x.Func == "Sửa Fillet / Hạn Mức Trọng Lượng" && x.Status == 1).FirstOrDefault();
                            if (suaHanMucTrongLuongFillet != null)
                            {
                                <button onclick="showUpdate()" style="margin-right:5px" class="btn btn-outline-warning"><i class='bx bx-edit-alt'></i>Sửa</button>
                            }
                            var xoaHanMucTrongLuongFillet = rolePermistions.Where(x => x.Fu == "Danh Mục / Fillet / Hạn Mức Trọng Lượng" && x.Func == "Xoá Fillet / Hạn Mức Trọng Lượng" && x.Status == 1).FirstOrDefault();
                            if (xoaHanMucTrongLuongFillet != null)
                            {
                                <button onclick="showDelete()" style="margin-right:5px" class="btn btn-outline-danger"><i class='bx bx-trash'></i>Xoá</button>
                            }
                            
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>

<div class="modal fade" id="modalThem" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Thêm Hạn Mức Trọng Lượng Fillet</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="card">
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-12 mb-12 ">
                                <div class="form-outline">
                                    <label class="form-label">STT</label>
                                    <input type="text" id="sttHMTLFL" class="form-control form-control-lg" autocomplete="off" placeholder="Nhập STT..." style="text-transform: uppercase;" disabled />
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-12 mb-12 ">
                                <div class="form-outline">
                                    <label class="form-label">Ngày</label>
                                    <input type="date" id="ngayHMTLFL" class="form-control form-control-lg" />
                                </div>
                            </div> 
                        </div>

                        <div class="row">
                            <div class="col-md-12 mb-12 ">
                                <div class="form-outline">
                                    <label class="form-label">Mã Lô</label>
                                    <select id="maLoHMTLFL" class="form-control form-control-lg">
                                        <option disabled selected>Chọn Lô</option>
                                        @foreach (var item in listMaLos)
                                        {
                                            <option value="@item.Item2">@item.Item2</option>
                                        }
                                    </select>
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-12 mb-12 ">
                                <div class="form-outline">
                                    <label class="form-label">Thành Phẩm</label>
                                    <select id="maThanhPhamHMTLFL" class="form-control form-control-lg">
                                        <option disabled selected>Chọn Thành Phẩm</option>
                                        @foreach (var item in listTPFLs)
                                        {
                                            <option value="@item.Ma">@item.Ten</option>
                                        }
                                    </select>
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-12 mb-12 ">
                                <div class="form-outline">
                                    <label class="form-label" for="x1">Trọng Lượng</label>
                                    <input type="number" min="0" step="0.01" id="trongLuongHMTLFL" class="form-control form-control-lg" autocomplete="off" placeholder="0.00" />
                                </div>
                            </div>
                        </div>
                        <div class="row mt-4 pt-2">
                            <div class="col-12">
                                <button onclick="insert()" class="btn btn-primary w-100 position-relative" type="submit" id="">
                                    <div class=" align-items-center">
                                        <span id="txtThem">Thêm</span>
                                        <div id="loadingIndicator" class=" spinner-border text-secondary position-absolute" role="status" style="left: 0;right: 0;top: 0;bottom: 0;margin: auto; display:none">
                                            <span class="visually-hidden">Đang tải...</span>
                                        </div>
                                    </div>
                                </button>
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="modal fade" id="modalSua" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Sửa Hạn Mức Trọng Lượng Fillet</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="card">
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-12 mb-12 ">
                                <div class="form-outline">
                                    <label class="form-label">STT</label>
                                    <input type="text" id="sttHMTLFLSua" class="form-control form-control-lg" autocomplete="off" placeholder="Nhập STT..." style="text-transform: uppercase;" disabled />
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-12 mb-12 ">
                                <div class="form-outline">
                                    <label class="form-label">Ngày</label>
                                    <input type="date" id="ngayHMTLFLSua" class="form-control form-control-lg" disabled />
                                </div>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-12 mb-12 ">
                                <div class="form-outline">
                                    <label class="form-label">Mã Lô</label>
                                    <select id="maLoHMTLFLSua" class="form-control form-control-lg">
                                        <option disabled selected>Chọn Lô</option>
                                        @foreach (var item in listMaLos)
                                        {
                                            <option value="@item.Item2">@item.Item2</option>
                                        }
                                    </select>
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-12 mb-12 ">
                                <div class="form-outline">
                                    <label class="form-label">Thành Phẩm</label>
                                    <select id="maThanhPhamHMTLFLSua" class="form-control form-control-lg">
                                        <option disabled selected>Chọn Thành Phẩm</option>
                                        @foreach (var item in listTPFLs)
                                        {
                                            <option value="@item.Ma">@item.Ten</option>
                                        }
                                    </select>
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-12 mb-12 ">
                                <div class="form-outline">
                                    <label class="form-label" for="x1">Trọng Lượng</label>
                                    <input type="number" min="0" step="0.01" id="trongLuongHMTLFLSua" class="form-control form-control-lg" autocomplete="off" placeholder="0.00" />
                                </div>
                            </div>
                        </div>
                        <div class="row mt-4 pt-2">
                            <div class="col-12">
                                <button onclick="update()" class="btn btn-primary w-100 position-relative" type="submit" id="">
                                    <div class=" align-items-center">
                                        <span id="txtSua">Sửa</span>
                                        <div id="loadingIndicatorSua" class=" spinner-border text-secondary position-absolute" role="status" style="left: 0;right: 0;top: 0;bottom: 0;margin: auto; display:none">
                                            <span class="visually-hidden">Đang tải...</span>
                                        </div>
                                    </div>
                                </button>
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<input type="hidden" id="sttHMTLFLRowSelected" />
<input type="hidden" id="ngayHMTLFLRowSelected" />
<input type="hidden" id="maxuongHMTLFLRowSelected" />
<input type="hidden" id="xuongIdHMTLFL" value="@XuongIdHMTLFL" />
<script>
    $(document).ready(function () {
        loadGridData();
    });
    var grid;
    var now = new Date();

    var day = ("0" + now.getDate()).slice(-2);
    var month = ("0" + (now.getMonth() + 1)).slice(-2);

    var ngay = now.getFullYear() + "-" + (month) + "-" + (day);
    var maXuong = $('#xuongIdHMTLFL').val();
    function loadGridData() {
        $.ajax({
            url: '/HanMucTrongLuongFillet/GetAllsFullField',
            type: 'GET',
            //dataType: 'json',
            data: {
                ngay: ngay,
                maXuong: maXuong
            },
            success: function (data) {
                var dataManager = new ej.data.DataManager({
                    json: data,
                    adaptor: new ej.data.JsonAdaptor()
                });
                if (grid) {
                    grid.dataSource = dataManager;
                    grid.refresh();
                } else {
                    grid = new ej.grids.Grid({
                        dataSource: dataManager,
                        allowPaging: true,
                        pageSettings: { pageSize: 50, pageSizes: [50, 100, 200, 500] },
                        allowFiltering: true,
                        filterSettings: { type: 'Excel' },
                        allowSorting: true,
                        //frozenColumns: 3,
                        allowResizing: true,
                        allowExcelExport: true,
                        toolbar: ['ExcelExport'],
                        rowSelected: onRowSelected,
                        height: 400,
                        columns: [
                            { field: 'STT', headerText: 'STT', width: 50 },
                            { field: 'Ngay', headerText: 'Ngày', width: 150, type: 'date', format: 'dd-MM-yyyy' },
                            { field: 'Gio', headerText: 'Giờ', width: 150 },
                            { field: 'MaLo', headerText: 'Lô', width: 150 },
                            { field: 'ThanhPhamName', headerText: 'Thành Phẩm', width: 150 },
                            { field: 'TrongLuong', headerText: 'Trọng Lượng', type: 'number', width: 150 },
                            { field: 'XuongName', headerText: 'Xưởng', width: 150 },
                        ]
                    });
                    // Hiển thị Grid trên trang web
                    grid.appendTo('#Grid');
                    grid.toolbarClick = function (args) {
             if (args.item.id === 'Grid_excelexport') {
                grid.excelExport();
             }};
                }
            },
            error: function (error) {
                console.error(error);
            }
        });
    }
    loadGridData();
    function reloadData() {
        loadGridData();
    }
    function onRowSelected(args) {
        // Lấy dữ liệu của hàng được chọn
        var selectedRecord = args.data;
        // Lấy giá trị Id từ dữ liệu của hàng được chọn
        var stt = selectedRecord.STT;
        var ngay = selectedRecord.Ngay;
        var maxuong = selectedRecord.MaXuong;
        $('#sttHMTLFLRowSelected').val(stt);
        $('#ngayHMTLFLRowSelected').val(ngay);
        $('#maxuongHMTLFLRowSelected').val(maxuong);

    }
</script>
<script>
    function chuyenDoiNgay(inputDate) {
        // Chuyển đổi kiểu dữ liệu của inputDate thành đối tượng Date
        var dateObject = new Date(inputDate);

        // Lấy ngày, tháng, năm từ đối tượng Date
        var day = dateObject.getDate();
        var month = dateObject.getMonth() + 1; // Tháng bắt đầu từ 0
        var year = dateObject.getFullYear();

        // Định dạng lại ngày, tháng, năm theo định dạng yyyy-MM-dd
        var formattedDate = year + "-" + (month < 10 ? '0' : '') + month + "-" + (day < 10 ? '0' : '') + day;

        // Trả về ngày đã định dạng
        return formattedDate;
    }
    function showInsert() {
        $.ajax({
            type: "GET",
            url: "/HanMucTrongLuongFillet/CreatDefautNew",
            data: {
                ngay: ngay,
                maXuong: maXuong
            },
            success: function (rs) {
                if (rs.isSuccess === true) {
                    $('#sttHMTLFL').val(rs.STT);
                    $('#ngayHMTLFL').val(chuyenDoiNgay(rs.Ngay));
                  
                    $('#modalThem').modal('show');
                }
                else {
                    toastOptionNotyfi();
                    toastr["warning"](rs.Mesages, "Thông báo");
                }
            },
            error: function () {
                toastOptionNotyfi();
                toastr["error"]('Đã xảy ra lỗi: ' + error, "Thông báo");
            }
        });
    }
    function insert() {
        var stt = $('#sttHMTLFL').val();
        var ngay = $('#ngayHMTLFL').val();
        var maLo = $('#maLoHMTLFL').val();
        var maThanhPham = $('#maThanhPhamHMTLFL').val();
        var trongLuong = $('#trongLuongHMTLFL').val();

        document.getElementById("txtThem").style.visibility = "hidden";
        document.getElementById("loadingIndicator").style.display = "block";
        $.ajax({
            type: 'POST',
            url: '/HanMucTrongLuongFillet/DoInsert',
            data: {
                stt: stt,
                ngay: ngay,
                maLo: maLo,
                maThanhPham: maThanhPham,
                trongLuong: trongLuong,
                maXuong: maXuong
            },
            success: function (rs) {
                if (rs.isSuccess === true) {
                    toastOptionNotyfi();
                    toastr["success"](rs.Messages, "Thông báo");
                    $('#modalThem').modal('hide');
                    reloadData();
                } else {
                    toastOptionNotyfi();
                    toastr["warning"](rs.Messages, "Thông báo");
                }
            },
            error: function (error) {
                toastOptionNotyfi();
                toastr["error"]('Đã xảy ra lỗi: ' + error, "Thông báo");
            },
            complete: function () {
                document.getElementById("loadingIndicator").style.display = "none";
                document.getElementById("txtThem").style.visibility = "visible";
            }
        });
    }

    function showUpdate() {
        var stt = $('#sttHMTLFLRowSelected').val();
        var ngay = $('#ngayHMTLFLRowSelected').val();
        var maXuong = $('#maxuongHMTLFLRowSelected').val();

        $.ajax({
            url: "/HanMucTrongLuongFillet/GetsByMa",
            data: {
                stt: stt,
                ngay: ngay,
                maXuong: maXuong,
            },
            type: 'GET',
            dataType: 'json',
            success: function (rs) {
                if (rs.isSuccess === true) {
                    $('#sttHMTLFLSua').val(rs.STT);
                    $('#ngayHMTLFLSua').val(chuyenDoiNgay(rs.Ngay));
                    $('#maLoHMTLFLSua').val(rs.MaLo);
                    $('#maThanhPhamHMTLFLSua').val(rs.MaThanhPham);
                    $('#trongLuongHMTLFLSua').val(rs.TrongLuong);
                    $('#modalSua').modal('show');
                }
                else {
                    toastOptionNotyfi();
                    toastr["warning"](rs.Mesages, "Thông báo");
                }
            },
            error: function (error) {
                toastOptionNotyfi();
                toastr["error"]('Đã xảy ra lỗi: ' + error, "Thông báo");
            }
        });
    }
    function update() {
        var stt = $('#sttHMTLFLSua').val();
        var ngay = $('#ngayHMTLFLRowSelected').val();
        var maLo = $('#maLoHMTLFLSua').val();
        var maThanhPham = $('#maThanhPhamHMTLFLSua').val();
        var trongLuong = $('#trongLuongHMTLFLSua').val();
        document.getElementById("txtSua").style.visibility = "hidden";
        document.getElementById("loadingIndicatorSua").style.display = "block";
        $.ajax({
            url: '/HanMucTrongLuongFillet/DoUpdate',
            data: {
                stt: stt,
                ngay: ngay,
                maLo: maLo,
                maThanhPham: maThanhPham,
                trongLuong: trongLuong,
                maXuong: maXuong
            },
            type: 'POST',
            dataType: 'json',
            success: function (rs) {
                if (rs.isSuccess === true) {
                    $('#modalSua').modal('hide');
                    reloadData();
                    toastOptionNotyfi();
                    toastr["success"](rs.Messages, "Thông báo");
                } else {
                    toastOptionNotyfi();
                    toastr["warning"](rs.Messages, "Thông báo");
                }
            },
            error: function (error) {
                toastOptionNotyfi();
                toastr["error"]('Đã xảy ra lỗi: ' + error.toString(), "Thông báo");
            },
            complete: function () {
                document.getElementById("loadingIndicatorSua").style.display = "none";
                document.getElementById("txtSua").style.visibility = "visible";
            }
        });
    }

    function showDelete() {
        var stt = $('#sttHMTLFLRowSelected').val();
        var ngay = $('#ngayHMTLFLRowSelected').val();
        var maXuong = $('#maxuongHMTLFLRowSelected').val();

        $.ajax({
            url: "/HanMucTrongLuongFillet/GetsByMa",
            data: {
                stt: stt,
                ngay: ngay,
                maXuong: maXuong
            },
            type: 'GET',
            dataType: 'json',
            success: function (rs) {
                if (rs.isSuccess === true) {
                    bootbox.confirm({
                        title: "Thông báo!",
                        message: "Bạn có chắc muốn xoá ! ",
                        buttons: {
                            confirm: {
                                label: 'OK',
                                className: 'btn btn-primary'
                            }
                        },
                        callback: function (result) {
                            if (result) {
                                //Người dùng đã bấm OK, Gọi API xóa ở đây
                                $.ajax({
                                    url: '/HanMucTrongLuongFillet/DoDelete',
                                    data: {
                                        stt: stt,
                                        ngay: ngay,
                                        maXuong: maXuong
                                    },
                                    type: 'POST',
                                    dataType: 'json',
                                    success: function (rs) {
                                        if (rs.isSuccess === true) {
                                            reloadData();
                                            toastOptionNotyfi();
                                            toastr["success"](rs.Messages, "Thông báo");
                                        } else {
                                            toastOptionNotyfi();
                                            toastr["warning"](rs.Messages, "Thông báo");
                                        }
                                    },
                                    error: function (error) {
                                        toastOptionNotyfi();
                                        toastr["error"]('Đã xảy ra lỗi: ' + error.toString(), "Thông báo");
                                    }
                                });
                            }
                        }
                    });
                } else {
                    toastOptionNotyfi();
                    toastr["warning"]('Không tìm thấy thông tin!', "Thông báo");
                }
            },
            error: function (error) {
                toastOptionNotyfi();
                toastr["error"]('Đã xảy ra lỗi: ' + error.toString(), "Thông báo");
            }
        });
    }
</script>




